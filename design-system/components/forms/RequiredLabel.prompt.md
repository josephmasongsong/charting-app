Form field label that appends a red asterisk for required fields — pair with Input/Select/Textarea/Radio/Checkbox.

```jsx
<RequiredLabel required>Legal First Name:</RequiredLabel>
<Input placeholder="Label" />
```

`bold` renders question-style labels (numbered form questions) at 700 weight; default is 400.

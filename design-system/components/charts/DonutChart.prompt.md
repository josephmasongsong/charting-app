Donut breakdown chart (Recharts `PieChart`) in Power BI house style (teal ramp: dark → seafoam → light).

```jsx
<DonutChart data={[{name:"Administrative",value:207,color:"var(--chart-3)"},{name:"Excluded",value:106,color:"var(--chart-1)"}]} />
```

56px app shell header with brand mark + wordmark, variant-driven background. Use `blue` on pages whose main content is a form (creation, settings); `teal` everywhere else.

```jsx
<AppHeader variant="teal" right={<nav>Home · Feedback Forms</nav>} />
```

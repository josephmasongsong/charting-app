The BC Housing roof-in-colour-wheel mark, embedded as a data URI so it resolves from any page. Renders at 30px by default; pass `size` to scale. `dark` is retained for API compatibility and has no visual effect.

```jsx
<BrandMark size={30} />
```

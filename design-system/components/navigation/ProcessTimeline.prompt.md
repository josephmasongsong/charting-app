Compact numbered-dot milestone tracker (PartnerHub operational review flow).

```jsx
<ProcessTimeline steps={[{label:"Initiate",state:"done"},{label:"Report",state:"current"},{label:"Close",state:"pending"}]} />
```

Fixed 232px teal sidebar shell for the PartnerHub app — nav only, never actions on content. Expandable groups render children on a teal-700 inset panel.

```jsx
<Sidebar activeItem="Standards and Elements" onSelect={setActive} />
```
